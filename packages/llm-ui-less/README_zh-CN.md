# @llm/ui-less (简体中文)

LLM 聊天 UI 库的轻量级版本，使用 Less 进行样式开发。

## 功能特性

- **轻量级**: 优化性能，依赖更少。
- **Less 样式**: 使用 Less 进行灵活且易于维护的样式开发。
- **核心组件**: 包含与主 UI 库兼容的核心聊天组件。

## 安装

```bash
pnpm add @llm/ui-less
```

## 使用

```typescript
import { ChatMain } from "@llm/ui-less";
// ...
```
