module.exports = {
  // keep print width consistent with ESLint `max-len`
  printWidth: 120,
  proseWrap: "never",
  // Follow existing repo style (double quotes)
  singleQuote: false,
  // No trailing commas to avoid noisy diffs in some targets
  trailingComma: "none",
  semi: true,
  tabWidth: 2,
  useTabs: false
};
